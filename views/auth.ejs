<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Tribook</title>
        <meta charset="UTF-8">
        <%- include('./partials/head.ejs') %>
    </head>
    <style>
        .mySlides {display: none}
    </style>
    <body style="padding-top: 50px;"> 
        <main class="container" style="max-width: 500px;">
            <h1 class="w3-center">Iniciar sesión</h1>
            <!-- Formulario de inicio de sesión -->
            <form class="w3-container w3-card-4 w3-white w3-padding-32 w3-margin-top" method="POST" action="/login">  
                <label class="w3-text-teal"><i class="fa fa-user"></i> Usuario</label>
                <input class="w3-input w3-border w3-margin-bottom" type="text" name="username" placeholder="Introduce tu usuario" required>
                <label class="w3-text-teal"><i class="fa fa-lock"></i> Contraseña</label>
                <input class="w3-input w3-border w3-margin-bottom" type="password" name="password" placeholder="Introduce tu contraseña" required>  
                <button class="w3-button w3-blue w3-teal" type="submit">Iniciar sesión</button>
            </form>
            <!-- Mensaje de error -->
            <% if (error_msg) { %>
            <div class="w3-panel w3-margin-top">
                <p><%= error_msg %></p>
            </div>
            <% } %>
            <p class="w3-center w3-margin-top">
                Clica <a style="text-decoration: underline;" href="/">aquí</a> para regresar a la página principal.
            </p>
        </main>
    </body>
</html>